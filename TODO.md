# TODO - Responsive / No-scroll layout

- [ ] Add responsive layout rules to `frontend/src/styles/app.css` (sidebar collapse, padding scaling, table handling).
- [ ] Ensure `main-content` stays within viewport without causing page scroll (use overflow rules + body sizing).
- [ ] Improve table behavior on small screens (horizontal scroll inside table wrapper, reduced cell padding/font).
- [ ] Run frontend build/lint (if available) and do a quick visual sanity check.

